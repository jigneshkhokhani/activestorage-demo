# How to use activestorage with rails >=5.1

Add Gem in gemfile

```
gem 'activestorage'
```

Enable gem which is use for image processing

```
# Use ActiveStorage variant
gem 'mini_magick', '~> 4.8'
```